 <div id="page-wrapper">

            <div class="container-fluid">

                <!-- Page Heading -->
                <div class="row">
                    <div class="col-lg-12">
                        <h1 class="page-header">
                            Registrar Negocio
                        
                        </h1>
                        

                    </div>
                </div>
                <!-- /.row -->

                <div class="row">
                    <%= form_for :postClasification do |f| %>
                       
                        <div class="form-group col-lg-12">
                             <%= f.label :Imagen_Principal %>:<br>
                             <%= f.file_field :Principal, :class=>"file" %>
                             <br>
                        </div>
                        <hr>
                        <div class="form-group col-lg-12">
                            <%= f.label :Galeria_De_Imagenes %>:<br><br>
                            <%= f.file_field :Principal, :class=>"file", :multiple=>"true data-preview-file-type='any'" %>
                            <hr><br>
                        </div>
                        <div class="form-group">
                            <br><br>
                            <div class=" ">
                                <%= f.label :Clasificación %>:<br><br>
                                <% @clasifications.each do |clasification| %>
                                    <p><%= f.check_box(clasification.Name) %>
                                    <%= f.label(clasification.Name, clasification.Name) %></p>
                                <% end %>
                                <br><br>
                            </div>
                        </div><hr>
                        <div class="form-group">
                            <br><br>
                            <div class="">
                                <%= f.label :Tipos %>:<br><br>
                                <% @types.each do |type| %>
                                    <p><%= f.check_box(type.Name) %>
                                    <%= f.label(type.Name, type.Name) %></p>
                                <% end %>
                                <br><br>
                            </div>
                        </div>
                        <%= f.submit :Guardar ,:class=>"btn btn-info" %>
                        
                        <!-- /.row -->
                    <% end %>
                </div>
            </div>
                <!-- /.container-fluid -->

        </div>
        <!-- /#page-wrapper -->

    </div>
        </div>
        </div>
        </div>